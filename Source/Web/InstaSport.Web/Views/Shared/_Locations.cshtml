@model IEnumerable<InstaSport.Web.ViewModels.Home.LocationViewModel>

<div class="row">
    @foreach (var location in Model)
    {
        <div class="col-md-4">
            @Html.DisplayFor(m => location)
        </div>
    }
</div>